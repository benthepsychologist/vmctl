# openclaw-gateway Agent Configuration
#
# Copy this file to the VM:
#   scp agent.env.example VM:/srv/vmctl/agent/openclaw-gateway/secrets/agent.env
#
# Or edit directly on VM:
#   vmctl ssh -- 'sudo nano /srv/vmctl/agent/openclaw-gateway/secrets/agent.env'
#
# After editing, restart the container:
#   vmctl restart --app-dir /srv/vmctl/apps/openclaw-gateway
#
# File permissions: chmod 600, owned by root

# ============================================================================
# REQUIRED: Azure OpenAI (for LLM access via local proxy)
# ============================================================================
# Get these from Azure Portal → OpenAI resource → Keys and Endpoint
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_HOST=your-resource.openai.azure.com
AZURE_OPENAI_API_VERSION=2024-10-21

# ============================================================================
# OPTIONAL: Channel integrations (add only what you need)
# ============================================================================
# Discord webhook for notifications
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Telegram bot for messaging
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# TELEGRAM_CHAT_ID=-1001234567890

# ============================================================================
# OPTIONAL: BigQuery sync configuration
# ============================================================================
# Project and dataset for session sync/feed (defaults shown)
# MOLT_BQ_PROJECT=molt-chatbot
# MOLT_BQ_DATASET=molt

# ============================================================================
# DO NOT ADD (security boundary - these stay in life/dev zones):
# ============================================================================
# - SSH keys or Git tokens
# - GCP service account keys
# - AWS credentials
# - Personal API tokens
