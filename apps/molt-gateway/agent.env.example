# molt-gateway Agent Secrets
#
# This file contains ONLY explicitly granted channel tokens.
# Copy to /srv/vmctl/agent/molt-gateway/secrets/agent.env on the VM.
#
# SECURITY RULES:
# - Do NOT add cloud credentials (GCP, AWS, Azure)
# - Do NOT add SSH keys or Git tokens
# - Do NOT add workstation environment variables
# - Only add tokens explicitly granted for agent use
#
# File should be: chmod 600, owned by root

# Example granted tokens (replace with actual values):
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# TELEGRAM_CHAT_ID=-1001234567890
